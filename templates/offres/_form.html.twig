{{ form_start(form) }}
    <div class="form-row">
      <div class="form-group col-md-8">
        <label for="inputEmail4">Titre</label>
        {{ form_row(form.titre) }}
      </div>
      <div class="form-group col-md-6">
        <label for="inputEmail4">Description</label>
        {{ form_row(form.description) }}
      </div>
      <div class="form-group col-md-6">
        <label for="inputEmail4">Métier</label>
        {{ form_row(form.metier) }}
      </div>
      <div class="form-group col-md-6">
        <label for="inputEmail4">URL</label>
        {{ form_row(form.url) }}
      </div>
      <input  type="submit" value="{{ submitButtonText ?? "Créer l'offre" }}" class="btn mt-2 btn-primary btn-block" formnovalidate>
      {{ form_end(form) }}
